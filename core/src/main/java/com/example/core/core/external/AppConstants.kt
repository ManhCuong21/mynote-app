package com.example.core.core.external

object AppConstants {
        const val PATH_MEDIA_NOTE = "MediaNote"
        const val PATH_IMAGE_NOTE = "ImageNote"
        const val PATH_RECORD_NOTE = "RecordNote"
        const val FILE_NAME_FORMAT = "yyyyMMddHHmmss"
        const val FILE_IMAGE_FORMAT = "yyyy-MM-dd HH:mm:ss"

        const val DATE_FORMAT_yyyy_MM_dd_HH_mm_ss = "yyyyMMddHHmmss"

        const val FORMAT_TIME_MINUTE = "mm:ss"
}