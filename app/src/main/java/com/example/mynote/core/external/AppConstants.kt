package com.example.mynote.core.external

class AppConstants {
    companion object {
        const val PATH_IMAGE_NOTE = "ImageNote"
        const val FILE_NAME_FORMAT = "yyyyMMdd HHmmss"
        const val FILE_IMAGE_FORMAT = "yyyy-MM-dd HH:mm:ss"
    }
}